public class ElevenGrade {
    int numStudents;
    int maxCapacity;
    
    public ElevenGrade(int numStudents, int maxCapacity){
        this.numStudents = numStudents;
        this.maxCapacity = maxCapacity;
    }
}

  
